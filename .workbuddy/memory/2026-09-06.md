# 2026-09-06 工作日志

## 文档体系重构：agent.md / ReadMe.md 去重与归属划分
- 用户要求两文档零重复：对外信息（功能/配置/技术栈/FAQ/更新日志）归 ReadMe，agent.md 只留 AI 内部信息 + 引用指针。
- agent.md 新增「文档写入决策流程」6 步判断：读者是谁 → ReadMe 是否已有该主题 → 是否 ReadMe 写错（直接修 ReadMe，不另存对照表）→ 两边都要（正文进 ReadMe）→ 版本号/更新日志正文只进 ReadMe → 「删掉还能干活就别写」。
- 移入 ReadMe：版本号规范（放「更新日志」开头）、真实在用的 CDN 客户端库、中继部署运维要点（FAQ 第 5 条补「push main 自动部署 + health 版本自证」）。
- 修正 ReadMe 过期事实：默认端口 3000→**5689**；删除已废弃的 `public/常用语.json`；技术栈移除 **FingerprintJS**（实测仅 `@require`、零调用点），补 `qrcodejs`/`Viewer.js`/`heic2any`；`DEFAULTS` 示例与代码对齐（补 `useCdn`、`commonPhrasesUrl` 改 GitHub 网页链接、`relayServer` 默认值 `https://znhd.122050.xyz`）。
- agent.md 删除「依赖实际用途」「代码与 ReadMe 的已知出入」两章（前者归 ReadMe 技术栈，后者根因是 ReadMe 过期已就地修正）。
- **纠旧说法**：agent.md 原部署叙述（stop/start 重绑 bind）已过时——`deploy.yml` 现为「tar 管道直写容器 `/app` + `docker restart`」，且注释明确 restart **不**重绑 bind 挂载。部署流程不再在 agent 复述，改为指针指向 `deploy.yml`（避免二次失真）。
- 按用户要求删除 agent.md 的「修改记录」章节及「更新要求」里的对应表述。

## agent.md 新增「工作区记忆（读写）」规则
- 位置 `.workbuddy/memory/`；`YYYY-MM-DD.md`（追加式、不覆盖）+ `MEMORY.md`（长期、按主题、3,000 字符/会话）；完成实质性工作后必须写入；30 天以上日志整理归档进 `MEMORY.md`。
- 同步把「读取顺序」第 ③ 步改为精确指向 `.workbuddy/memory/`。

## 处理历史代码审查遗留项（`.workbuddy/reviews/znhd-userjs-review.md`，共 15 项）
- **此前已解决 5 项**：语音队列上限（MAX_SPEECH_QUEUE=10 + TTL 30s + 关语音清空）、常用语 2 小时本地缓存、`escapeHtml` JSDoc、全量 32 函数 JSDoc、隐式返回 undefined（`appendToTinyMCE`）。
- **本次修复 8 项**：
  - 日志去重：只比对上一条 → `RECENT_LOG_COUNT=5` 最近 N 条窗口（修掉「每 3 秒一条」的间隔性重复刷屏）。
  - `appendToTinyMCE(text2append = 'xxxxx')` → `''`（不再掩盖漏传 bug），JSDoc 明确「失败返回空串」。
  - 面板位置跟踪：`beforeunload` 统一断开 `MutationObserver` + 移除 `resize` 监听（新增 `resizeHandler` 句柄）；拖拽 `mouseup` 改 `{ once: true }`。
  - `getShadowHosts()` 结果缓存（仅缓存**非空**结果，空结果不固化，避免面板未插入时永久失效），去掉定时器里的反复全树遍历。
  - 主面板组件 `DM` → `MainPanel`（3 处引用同步）。
  - `playDidaSound` 结构性异常记 warning 日志；`play()` 被自动播放策略拒绝仍静默（预期行为，已注释）。
  - 掉线双选择器 `:nth-child(2)` / `:nth-of-type(2)` 补「互补兜底」注释：页面结构差异时命中不同元素，非冗余、不合并。
- **判定有意保留 2 项**：`@match https://example.com/*`（面板/弹窗调试宿主）、`@connect *`（中继地址由用户自定义，必须通配）——已在 `// ==/UserScript==` 下方加注释说明，勿再当问题处理。
- **未做 4 项功能增强**：语音音量/语速可调、常用语分组、日志导出、用 MutationObserver 替代 3 秒轮询（涉及重构轮询机制，与「不得无理由重构可运行逻辑」冲突）——等用户确认。
- `@version` `26.7.29-v11` → `26.9.6-v1`；`node --check` 两文件通过；ReadMe 更新日志已补条目。

## 记忆维护
- 补写本日日志（此前漏写，原因见下）；`2026-07-18`~`2026-07-30` 共 7 份日志已超 30 天，按规则把关键长期事实蒸馏进 `MEMORY.md`（剪贴板图片唯一可靠路径、中继广播架构与反向通道、排查方法论、文档体系分工）。
- 原始 7 月日志保留未删（整理 ≠ 删除，删文件需用户确认）。

## 全量代码审查（用户要求「检查是否有改进点」，未改代码，供下一轮采纳）
- 审查范围：`znhd.user.js` 全 2547 行 + `relay-server/server.js` 全 930 行；两文件 `node --check` 通过。工作区含未提交改动 = relay v26.9.6-v1 `createChannel` 工厂化重构（ReadMe 更新日志已记录），非新问题。
- 已复核确认的高价值项（择要）：
  1. [中] `znhd.user.js` `checkCount`(~1352)：等待人数 >0 时**每 3s 无条件 `speak("有人来了")`**——只有 =0 分支按变化门控，>0 分支没门控、speak 也无去重 → 有人持续等待时会一直重复语音播报（与更新日志「人数不变不重复播报」的宣称相悖）。
  2. [中] `loadPhrasesData`(793-795)：`jsyaml.load('')/空体` 返回 undefined/null，直接 `setPhrasesData(data)` → 抽屉渲染 `Object.keys(phrasesData)`(655) 抛 TypeError（空文件/空 200 即崩）。应 `data || {}`。
  3. [中] 电脑→手机发图 `confirmSendImage` 不压缩不设上限；服务端 `MAX_BODY`=12MB 掐 JSON 体，超限 `readBody` reject + `req.destroy()` → 客户端只看到「网络错误」。应客户端预检/压缩 + 服务端回 413。
  4. [低] `server.js deliver` 先 shift 队头再筛 writable 目标，若目标全已 ended 会丢队头（窄窗口）；应先确认≥1 可写目标再 shift。
  5. [低] `CommonPhrasesDrawer`(635) `decodeURIComponent(resolveGithubUrl(...))`：用户可配 URL 含游离 `%` 时抛 URIError 崩抽屉渲染。
  6. [低] `loadAllvalue` 浅合并：存的 `workingHours` 部分对象会整体顶掉 DEFAULTS 其余字段成 undefined。
  7. [低] `startPhoneReceive.loggedConnFail` 从不复位；`showTextPopup` 复制按钮无条件显示「已复制」；`PhoneImageDrawer` 的 `onChangeRelayServer` 解构后未用（死参）。
- 死代码/文档：`znhd.user.js` 头部 `@require fingerprintjs@5`(24) 零调用可删；AGENT.md 仍把 relay 镜像列在「复用代码溯源/技术债务/关键数据结构」(58/66/84-85/127)，与已完成的 createChannel 工厂不符，需刷新；ReadMe CONFIG 示例(129-135) 漏 `MAX_SPEECH_QUEUE`/`SPEECH_QUEUE_TTL`、`didaUrl` 写法与代码(GitHub 网页链接)不符。
- 判定安全/可接受不处理：`@connect *`、`@match example.com`、双掉线选择器、deviceId 即无鉴权能力令牌（随机 UUID，属既有设计）、盲改弹窗 CSS/长轮询/剪贴板机制。
- 下一步待用户确认再改（版本号递增 + ReadMe 更新日志规则同前）。

## 落地修复（承接上节审查；用户选定修 2 项 + 语音重复播报维持现状不改）
- **语音播报重复**：用户明确「不需要改」，保留现状。
- **修 A（脚本）**：`loadPhrasesData` 空数据崩抽屉 → `jsyaml.load(...) || {}` 与缓存命中路径 `cache.data || {}`（`znhd.user.js`）。
- **修 B（脚本+服务端）**：电脑→手机发图超限/报错难懂 →
  - 服务端 `readBody` 超 `MAX_BODY` 改「读完丢弃、end 统一 reject{statusCode:413}」，`parseItemBody` 映射 413 JSON `{error:'内容过大…'}`（旧 `req.destroy()` 掐连接→客户端只见「网络错误」）；`server.js`。
  - 脚本新增 `RELAY_MAX_BODY`+`imagePayloadBytes()` 发前体积预检友好报错，`sendToPhone` 按 413/状态码给可读提示。
  - 已本地起服端到端冒烟：正常 200、超限 HTTP 413 且带中文 JSON body、413 后进程正常（`/phone/send` 仍 200）。手机上传页走同一 `parseItemBody` 亦自动受益。
- **版本**：`znhd.user.js` `@version`→`26.9.6-v4`；`relay-server/package.json`→`26.9.6-v2`；`node --check` 两文件通过；ReadMe 更新日志顶部已补两条目。
- **文档同步**：ReadMe CONFIG 示例补齐 `MAX_SPEECH_QUEUE`/`SPEECH_QUEUE_TTL`、`didaUrl` 改 GitHub 网页链接写法；AGENT.md 刷新 relay 工厂化后的过期引用（复用代码溯源/内部执行流程/关键数据结构/技术债务「逐行镜像已消除」）。
