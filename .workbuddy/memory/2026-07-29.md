# 2026-07-29 工作日志

## 手机端收图改为弹窗查看（与脚本端一致）（00:26）
- 需求：脚本端「发送到手机」的图片，手机端原先显示在网页底部内联九宫格（非弹窗），与脚本端（PC 收到手机图弹画廊）行为不一致；要求改为弹窗。
- 改动 `relay-server/server.js` 手机上传页：
  - CSS：`#recvGrid` 不再 `display:none` 内联切换；新增 `#recvPopup`（固定全屏遮罩，z-index:9998，含标题栏+关闭+可滚动网格）与 `#recvBtn`（底部「查看收到的图片(N)」重开按钮）。
  - HTML：把 `<div id="recvGrid">` 移入 `#recvPopup` 弹窗内；页面底部改放 `#recvBtn`。
  - JS：`renderRecvGrid` 去掉 `recvGrid.style.display` 切换，改为同步 `#recvBtn` 文案/显隐；新增 `openRecvPopup/closeRecvPopup`，绑定弹窗 × 与空白点击关闭、按钮重开；`showReceived` 图片分支由 `scrollIntoView` 改为 `openRecvPopup()` 自动弹出。单图放大仍走 `openRecvImage`（z 9999，长按保存）。
- 版本：`relay-server/package.json` version `26.7.28-v6` → `26.7.29-v1`（跨天重置 v1）。
- 校验：写临时脚本抽取 `uploadPageHtml` 模板→渲染→抽 `<script>`→`new Function` 语法校验通过（规避模板字符串 `\\` 陷阱，校验渲染后而非源码）；已删临时脚本。
- 文档：`ReadMe.md` 更新日志顶部加 `relay-server v26.7.29-v1` 条目；第 259 行「收进下方九宫格画廊」改为「自动弹出画廊弹窗」。
- 注：`/health` 返回的版本号现在应是 `26.7.29-v1`，部署后 `curl 127.0.0.1:5689/health` 可确认。
