# 2026-07-27 工作日志

- 修复 relay-server 手机端图片选取强制拍照问题：`relay-server/server.js` 隐藏的 `<input id="file">` 原带 `capture="environment"`（强制后置摄像头），导致手机端一选图就进相机。已移除 `capture` 属性，改为从系统选择器选取（相册/拍照可选）。HTML 模板内嵌于 server.js，需重启 node 服务生效。
