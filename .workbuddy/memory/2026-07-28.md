# 2026-07-28 工作记录

## relay-server 增加访问日志（排查"访问端口无日志"）
- 根因：`server.js` 只记录业务事件（[连接]/[发送]/[投递]/[断开]），**没有请求级访问日志**。
  浏览器直接 `GET /` 或 `GET /health` 只返回页面/JSON 但不写日志，所以"访问了端口却看不到任何输出"是正常的。
- 改动：在 `http.createServer` 处理器顶部加入请求级访问日志——`[访问] 方法 URL` + `res.on('finish')`
  输出 `[响应] 状态码 方法 URL (耗时ms)`。过滤掉高频长轮询/心跳路径（/recv、/phone/recv、/phone/heartbeat）防刷屏。
- 诊断价值：加日志后若访问仍无输出 → 是网络/反代/端口映射没把流量打到 5689（请求根本没到服务）；
  若有 [访问]/[响应] 输出 → 服务已正常收流量。
- 运行环境：用户在 1panel/node:25.9.0 的 node 运行时用 `npm start`（`node server.js`）跑该服务，监听 0.0.0.0:5689。

## 手机端卡"正在连接" / PC 看不到在线设备 / 手机发不出图（07-28 排查中）
- 现象：手机打开 `/u/<id>` 能加载页面（GET 通），但 `POST /phone/heartbeat` 心跳从未成功 → 无 `[连接]` 日志、PC `GET /phone/status` 返回 online:false、手机发图/文也失败。三个症状同源：手机→中继的 **POST 不通**。
- 服务器心跳处理是「收到即回 200」，本身无 bug；问题在手机到中继的 POST 链路。
- 注意：访问日志 ACCESS_NOISE 过滤了 /phone/heartbeat，故 relay 日志看不到心跳的 [访问] 行属正常；要看成功标志 `[连接] 设备 … 已连接`。
- 怀疑方向（HTTPS 是默认，已排除明文/运营商缓存）：①1panel「网站」反代前的防盗链/访问控制/防CC 拦了 POST；②自建 nginx 反代没透传 POST 或开了请求体缓冲；③手机网络策略拦 POST。
- 待用户用桌面 Chrome 打开上传链接看 Network 里 POST /phone/heartbeat 的状态，并确认 relay 日志是否出现 `[连接]` 以定位是"POST 没到服务"还是"到了被拦"。
- 候选改进（用户确认后做）：放开 /phone/heartbeat 访问日志过滤；手机页改用 Promise.race 超时（不依赖 AbortController），8s 后明确报"连接超时"而非一直转圈。

## 决定性结论（07-28 已定位）
- 从外部环境直接 `curl -X POST` 测试 `https://znhd.122050.xyz/phone/heartbeat/<id>` 与 `POST /u/<id>` 均返回 `200 {"ok":true}`
  → **openresty→relay 的 POST 链路完全正常**，无 WAF 拦截、无 CSP（响应头只有 HSTS）。
- 抓取线上页面正文（11082B）确认内联 `<script>` 完整、`heartbeat();` 有调用 → 页面本身无损坏、无 CSP 拦脚本。
- 因此：问题 100% 在**手机端浏览器/WebView 或其网络**，不在服务器/反代/页面。
- 手机永久"正在连接"且 8s 后仍不翻"超时" → 推断手机 WebView 的 `AbortController` 未触发 abort，POST 被网络卡住即永久挂起。
- 已落地修复（server.js）：
  1. 心跳超时改用 `Promise.race` 硬性 8s 超时（不依赖 AbortController）；
  2. 心跳 `fetch` 加 `Content-Type: application/json` + body `{}`（兼容对空 POST 敏感的代理）；
  3. `.then` 里检查 `response.ok`，非 2xx 不再误显示"已连接"；
  4. 临时把 `/phone/heartbeat` 从 ACCESS_NOISE 过滤移除，便于在 `docker compose logs -f znhd` 确认手机 POST 是否到达。
- 验证：`node --check` 通过；抽取内联脚本 `INLINE_SYNTAX_OK`。
- 下一步：用户重新部署后在手机打开 /u/<id>，≤8s 内应翻成明确状态：
  - 显示"🟢 已连接" → 通了；
  - 显示"连接超时/连接失败" → 手机网络在丢弃/篡改 POST，建议换 WiFi↔移动数据、确认脚本设置里中继地址是 https:// 而非 http://、排除公司 WiFi 防火墙。
  - 同时中继日志若出现 `[访问] POST /phone/heartbeat` → POST 已到服务，问题在响应回程/代理；若仍无 → 手机网络根本没把 POST 送出去。

## 重新部署后全站 502（07-28 01:47 发现）
- 用户重新部署后，`https://znhd.122050.xyz` 所有路径（/u/<id>、/health）都返回 502 Bad Gateway（openresty）。
- 1 小时前同样的请求还是 200 → 是**这次重建容器把 openresty→relay 链路弄断了**（典型：端口映射丢失/变更，或 nginx upstream 指向的 127.0.0.1:5689 无服务）。
- relay 自身日志显示启动正常（listen 0.0.0.0:5689 成功），说明 relay 在跑但 openresty 够不着它——大概率是容器端口没映射到宿主机 5689，或容器网络变了。
- 该 502 也解释了"日志无 [访问] POST"：请求死在 openresty，从未到 Node。

## 真凶定案：模板字符串吞掉正则反斜杠（07-28 01:50 用真实浏览器定位）
- 用 browser-use 真实打开线上 /u/<id>：桌面 Chrome 同样卡"正在连接"，performance 资源为空（页面从未发过任何 fetch）。
- 注入 error 监听后刷新，抓到 `Uncaught SyntaxError: Unexpected token 'var' @ :150`。
- 根因：server.js 的手机页是**模板字符串**，内联 JS 写了 `match(/\/u\/(...)/i)`，模板字符串把 `\/` 转义成 `/`，
  浏览器收到 `match(//u/...)` → `//` 成行注释 → 语法错误 → **整段内联脚本死亡** → 心跳/轮询/发送全部不执行。
- 修复：模板内写成 `\\/`（输出 `\/`）。本地起服务抓渲染页确认输出正确，`new Function(渲染后脚本)` 语法通过。
- ⚠️ 教训（长期）：校验模板字符串内嵌的 JS，**必须校验渲染后的输出**，不能校验转义前的源码
  （之前 extract_check.js 校验源码通过，但浏览器拿到的是转义后的坏代码）。
- ✅ 02:05 用户重新部署后确认：手机端连接成功，整条链路（心跳/在线状态/收发）恢复正常，问题闭环。
- 待办（可选）：排查已结束，可把 `/phone/heartbeat` 重新加回 server.js 的 ACCESS_NOISE 过滤，避免每 8s 刷一条心跳日志；tmp/ 下的排查临时文件（znhd_body/live*.html、rendered*.html、up.js、extract_check.js）可清理。

## 中继服务端版本追踪（02:10 落地）
- `relay-server/package.json` version 从 `1.0.0` 改为 `26.7.28-v1`（沿用 znhd.user.js 的 `YY.M.D-vN` 规范，合法 semver）。
- `server.js` 新增 `VERSION`（require package.json），启动日志改为 `[中继服务] vX.X.X-vN 已启动`，
  `GET /health` 返回 `{ok:true, version}` —— 以后可直接 curl /health 确认线上跑的是哪版，解决"新代码到底部署没部署"的确认难题。
- 已本地起服务实测：/health 返回 `{"ok":true,"version":"26.7.28-v1"}`，启动日志带版本。规范已写入 MEMORY.md。

## 全面 bug 巡检（02:15，version → 26.7.28-v2）
- 已修 2 个确定 bug（均在 server.js）：
  1. `/phone/heartbeat` 加回 ACCESS_NOISE 过滤（排查遗留，之前每 8s 刷 2 行日志）；
  2. 手机页「复制文本」按钮：原 `try{ navigator.clipboard.writeText() }catch` 捕获不到 Promise reject，
     失败也显示"已复制"；且老 WebView 无 navigator.clipboard 会直接 TypeError。
     改为 .then/.catch + execCommand('copy') 隐藏 textarea 兜底。
- 已确认渲染后内联脚本语法 OK、`\/u\/` 正则反斜杠完好（用 grep 渲染输出直接核对）。
- 已知设计隐患（未改，与用户确认后再说）：
  1. pending/phonePending 每设备只有 1 个槽位：新条目覆盖旧条目（手机连发两张图、第一张没被取走时会丢）。
     目前 deliverToAll 在 POST 后立即广播，正常场景不触发；只在"无接收端在等 + 连发多条"时丢。
  2. `payload.text.slice(0, MAX_BODY)` 上限 12M 字符 ≈ 最大 36MB JSON 文本，但 readBody 已限 12MB body，实际无害。
  3. 手机页 pollRecv 无硬性超时（依赖 fetch 自身），WebView 卡住时反向收件可能停摆但心跳仍正常（状态显示不受影响）。

## 访问日志再降噪（02:24，version → 26.7.28-v3）
- 用户反馈 `/phone/status` 轮询日志太多（PC 端约每 5s 一次），已加入 ACCESS_NOISE 过滤：
  `/^\/(recv|phone\/recv|phone\/heartbeat|phone\/status)(\/|$)/i`。
- 已用 node 单测正则：/phone/status/<id> 跳过，/u、/health、/phone/send 正常记录；`node --check` 通过。

## 手机上传页多选图片（12:58，version → 26.7.28-v4）
- 需求：手机上传支持多选图片，最多 9 张。
- 手机页改动：`<input>` 加 `multiple`；items 数组 + 九宫格缩略图（`#grid`，可单张 × 删除、可分次追加，
  合计上限 MAX_PICK=9，超出提示忽略）；发送改逐张顺序上传（一张成功再发下一张，保证到达顺序），
  显示进度"发送中…(n/总数)"；失败即停、剩余保留可重试；`fileInput.value=''` 允许重复选同一批文件。
- 服务端改动：`pending` 由每设备单槽改为 **FIFO 队列**（消除了之前记录的"单槽覆盖丢图"设计隐患①的正向通道部分）：
  - `MAX_QUEUE=9`，超出丢最旧并记 `[丢弃]` 日志；
  - `deliverToAll` 每次只投递队头一条（长轮询每响应一条），电脑端收到即自动 poll() 取下一条 → 油猴脚本零改动；
  - `/recv` tick 里清队头过期条目后再投递。
- 注：phonePending（电脑→手机反向通道）仍是单槽，未动。
- 验证：`node --check` 通过；本地起服务抓渲染页 `new Function()` 语法 OK、`\/u\/` 反斜杠完好（grep 渲染输出核对）；
  端到端测试：连发 3 条按序取回→第 4 次 empty；连发 10 条 q1 被丢弃、取回队头为 q2。
- ReadMe.md 已同步：工作流程第 3 条 + 更新日志新增"relay-server v26.7.28-v4"条目。

## git 整理
- `.gitignore` 已含 `tmp/`（用户确认 tmp 不上传 git），但 `tmp/cat_ui_lib.tmp.js` 是先提交后忽略、一直被跟踪；
  已 `git rm --cached tmp/cat_ui_lib.tmp.js` 移出索引（本地文件保留），下次 commit 生效。
- `desktop.ini` 也被 git 跟踪，建议后续一并 `git rm --cached` + 加入 `.gitignore`。
